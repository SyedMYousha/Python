

Q1_ Write a program that calculates the area of a triangle using the formula A = (b * h) / 2. The program should prompt the user to enter the base and 
height of the triangle and then display the calculated area.


Q2_ Write a program that prompts the user to enter two floating-point numbers and then calculate their average. The program should display the result 
with two decimal places.


Q3_ Write a program that prompts the user to enter a radius and then calculates the volume and surface area of a sphere. The formulas for volume and 
surface area are V = (4/3) _ pi _ r^3 and A = 4 _ pi _ r^2, respectively.